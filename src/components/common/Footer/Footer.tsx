import './footer.scss';

const Footer = () => {
    return (
        <footer className="app-footer">
            <span className="footer-text">Developed with ❤️</span>
        </footer>
    );
};

export default Footer;
